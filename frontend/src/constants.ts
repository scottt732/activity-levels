/**
 * Values shared by more than one component, where two copies could drift apart.
 */

/** The engine's own default training window, used when the config does not name one. */
export const DEFAULT_MIN_DAYS = 14;
